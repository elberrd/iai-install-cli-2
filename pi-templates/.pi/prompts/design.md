---
description: Change the app's LAYOUT from references (images) — the reference's structure, our design system's identity
argument-hint: "[image(s) + screens in scope + description — I'll ask for whatever is missing]"
---
Read `.pi/skills/fia/SKILL.md` and the cookbook `.pi/skills/fia/cookbooks/design.md`, and follow it to the letter.

References/request: $@

The golden rule: **the image is STRUCTURE inspiration — colors, fonts and
components come from OUR design system** (theme + registry). In this order:

1. Read the component registry, the current screens and the Impeccable skill
   (if installed — it rules the motion).
2. Read the reference(s) as a designer: structure, hierarchy, density,
   patterns, motion — and confirm your reading to me in one sentence. References
   can also come from the examples shelf (`ai-docs/examples/<slug>/assets/`,
   Kind `design`) — check it before asking me for more images.
3. Short interview (ONE question at a time, with a recommendation): scope, what
   matters from the reference, what to preserve, motion level. Decision log
   (cookbook `.pi/skills/fia/cookbooks/decision-log.md`): `open design` before
   the first question, `log` each answer, `close` after step 5 with the applied
   scope as outcome.
4. Per-screen plan mapped to the registry (missing component → /component flow
   FIRST; want the image's colors? → /theme with preview). Wait for
   my OK.
5. Apply: contained → one FDA, then browser validation (in Claude Code,
   /test-ui runs it); broad → recommend turning it into
   tasks (issues with the plan in the briefs) and executing with /task or /goal.

Never copy the reference pixel by pixel; never touch the theme here.
