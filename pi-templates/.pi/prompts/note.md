---
description: Capture an idea into the inbox — zero questions, back to work
argument-hint: "\"the idea\""
---
Idea: $@

Append ONE line to `ai-docs/inbox.md` (create it with a `# Inbox` header if missing):

`- [ ] YYYY-MM-DD — <one-line idea> (context: <optional>)`

Rules: NO interview, NO analysis, NO implementation. Context in parentheses
only if I gave it. Confirm in one line ("noted → inbox") and stop — /feature,
/quick and /map read the inbox and promote items when the time comes.
