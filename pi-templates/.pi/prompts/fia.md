---
description: FIA — load the factory and show the project's state
---
Read `.pi/skills/fia/SKILL.md` and follow its rules from now on.

Then show me a short overview:

1. Available FDAs (`imp/fda_*.mjs`) in a one-line-each table
2. If `ai-docs/todos/task-master.md` exists: how many tasks pending/in-progress/done
3. Last recorded run (`npm run fda:sessions`), if any
4. The commands I can use: /idea, /grill, /prd, /map, /task, /goal, /feature,
   /bug, /quick (small change, fast lane), /note (idea → inbox, zero questions),
   /spec (durable spec), /example (external reference → examples shelf),
   /status, /evolve (evidence-backed system retrospective) — and
   /agents (or `npm run agents`), which opens the viewer's "Agents" tab
   (http://127.0.0.1:4600#agents) to see engine login status and change each FDA
   agent's engine, model, reasoning and `fallbacks:` chain (saved to
   imp/fia.config.yaml)

Don't execute anything yet — just the overview.
